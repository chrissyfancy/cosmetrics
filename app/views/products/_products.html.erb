<table>
  <% @products.each do |product| %>
      <tr>
      <td class="products">
        <%= link_to image_tag(product.product_photo, size: "150x150"), product_path(product) %><br>
        <%= link_to product.brand.upcase, product_path(product) %><br>
        <%= product.name %><br>
        <%= number_to_currency(product.price)%><br>
        <% if !current_user.nil? && current_user.role == "admin" %>
          <%= link_to '<i class="fa fa-pencil" aria-hidden="true"></i>'.html_safe, edit_product_path(product) %> |
          <%= link_to '<i class="fa fa-trash-o" aria-hidden="true"></i>'.html_safe, product, method: :delete, data: { confirm: 'Are you sure?' } %><br><br>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>
